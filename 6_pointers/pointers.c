#include <stdio.h>

// 62.Implenent a void minmax(int *a, int *b, int *min, int *max) function that takes two integer pointers a and b as input and assigns the smaller value to min and the larger value to max using call by reference. Write a main function to test it with different values.
// void minmax(int *a, int *b, int *min, int *max) { 
//   if ( *a > *b ) {
//     *max = *a;
//     *min = *b;
//   }
//   else {
//     *max = *b;
//     *min = *a;
//   }
// }

int main () {
  // 59.Write a program that declares an integer variable and a pointer to it. Assign a value and print it using the pointer.
  // int a = 8;
  // int *b = &a;
  // printf("%d", *b);
  // printf("%p", b);

  // 60.Write a program to change the value of an integer variable using a pointer and the  * operator.
  // int a = 8;
  // int *b = &a;
  // printf("%d\n", a);
  // *b = 9;
  // printf("%d\n", *b);
  // printf("%d\n", a);

  // 61.Declare a pointer to a char and use it to read and print a character entered by the user. 
  // char *name = "mahmood";
  // printf("welcome %s", name);
  // char character;
  // char *ptr = &character;
  // printf("Enter a character: ");
  // scanf(" %c", ptr);
  // printf("value of character: %c\n", character);
  // printf("value from pointer: %c\n", *ptr);

  // 62.Implement a void minmax(int *a, int *b, int *min, int *max) function that takes two integer pointers a and b as input and assigns the smaller value to min and the larger value to max using call by reference. Write a main function to test it with different values.
  // int a, b, min, max;
  // printf("Enter a number: ");
  // scanf("%d", &a);
  // printf("Enter b number: ");
  // scanf("%d", &b);
  // minmax(&a, &b, &min, &max);
  // printf("min number: %d\n", min);
  // printf("max number: %d\n", max);

  return 0;
}